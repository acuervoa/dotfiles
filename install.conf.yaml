- defaults:
    link:
      create: true
      relink: true

- clean: [ '~' ]

- link:
    ~/.dotfiles: .
    ~/.vim:
    ~/.vim/autoload: meta/vim-pathogen/autoload
    ~/.vimrc: vim/vimrc
- shell:
  - [ git submodule update --init --recursive, "Installing submodules" ]
