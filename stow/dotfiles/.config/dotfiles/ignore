# Dotfiles: paths to ignore (one per line). Comments allowed.
# runtime/state
.backups
.manifests
.cache
.local/state
.local/share/Trash

# common build/vendor
node_modules
dist
build
target
.venv
__pycache__

# git internals (noise)
.git

# common tooling caches
.direnv
.mypy_cache
.pytest_cache
.ruff_cache
.coverage
